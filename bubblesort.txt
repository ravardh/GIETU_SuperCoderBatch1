def bubblesort(arr):
  n = len(arr)
  for i in range(n-1):
    for j in range(i, n):
      if arr[i] > arr[j]:
        arr[i], arr[j] = arr[j], arr[i]
  return arr

arr = [32, 14, 44, 57, 12, 34, 89, 45]
bubblesort(arr)